/*
Make a simple function called greet that returns the most-famous "hello world!".
*/
let message = "hello world!";
let person = "Karina";

function greet() {
    if (person.length > 0) {
        return message;
    }
}